<?xml version="1.0" encoding="utf-8"?>
<!--
    SystemUI overlay — Doze/AOD behavior.
    All values are now generated from config.env via generated.xml:
      - doze_display_state_supported → HAS_AOD
      - doze_suspend_display_state_supported → DOZE_SUSPEND_UNSUPPORTED
      - doze_proximity_check_before_pulse → always false
      - doze_pickup_performs_proximity_check → always false
    Edit config.env and re-build to change.
-->
<resources>

    <!-- (Empty — all values generated dynamically) -->

</resources>
